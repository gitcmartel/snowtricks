<div class="row justify-content-center text-center mb-5">
    <div class="col-md-10">
        <div class="heading-39101 mb-5">
            <span class="backdrop text-center">{{ pageTitle }}</span>
            <span class="subtitle-39191">{{ pageTitle }}</span>
            <h3>{{ pageTitle }}</h3>
        </div>
    </div>
</div>