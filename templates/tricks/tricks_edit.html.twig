{% extends 'tricks/tricks.html.twig' %}

{% block form %}
    <form method="post" action="{{ path('app_tricks_edit', {'tricks': formTricks.vars.value.id }) }}"
        enctype="multipart/form-data" id="formTricks">
{% endblock %}
